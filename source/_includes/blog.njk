{% extends "base.njk" %}

{% block title %}
<title>{{ title }}</title>
{% endblock %}

{% block content %} 
<article>
<h1>{{title}}</h1>
<time datetime="{{ page.date.toISOString() | truncate(10,false,"") }}">{{page.date.toUTCString() | truncate(16,true,' ') }}</time>
	{{ content | safe }}
	
</article>
{% endblock %}
